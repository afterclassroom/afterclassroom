$("#rate_<%= @post.id %>").html("<%= raw(@text) %>");
vtip();
$(document).ready(function(){
  $('#rate_<%= @post.id %>').find('a').each(function(){
    var strLink = $(this).attr('href');
      $(this).parent().click(function(){
	ShowList_<%= @post.id %>("Rating summary");
      });
  });//end .each
});//end ready event
