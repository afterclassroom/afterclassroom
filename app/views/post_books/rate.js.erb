$("#rate_<%= @post.id %>").html("<%= raw(@text) %>");
vtip();
$(document).ready(function(){
  $('#rate_<%= @post.id %>').find('a').each(function(){
    var strLink = $(this).attr('href');
      $(this).click(function(){
        alert('Hien thi lai danh sach cac comment like-dislke');
      });
  });//end .each
});//end ready event
