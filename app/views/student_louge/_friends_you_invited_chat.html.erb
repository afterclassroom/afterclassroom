<ul id="friends_you_invited_to_chat">Friends you invited to chat chit
	<% for friend_invite in friends_invite %>
	<li>
		<%= image_tag friend_invite.user_target.avatar.url(:thumb), :onclick => "mydow_open('#{friend_invite.user_target.full_name}')" %>
	</li>
	<script>
		var <%= "window_" + friend_invite.user_target.login %>=new_mydow("<%= friend_invite.user_target.full_name %>");
		mydow_load(<%= "window_" + friend_invite.user_target.login %>.handle, "/student_louge/chanel_chat_content?user_id=<%= friend_invite.user_target.id %>");
	</script>
	<% end %>
<ul>