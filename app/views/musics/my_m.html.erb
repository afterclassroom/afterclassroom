<div class="slsTop">
  <div class="slsTopL">
    <div class="slsTopLT">Your photos</div>
  </div>
  <div class="slsTopR">
    <!--Pagination-->
    <div class="assPage friend_photos">
      <span class="numPage"><%= page_entries_info @musics %></span>
      <%= will_paginate @musics %>
    </div>
    <!--Pagination-->
  </div>
</div>
<div class="photoRow">
  <% @musics.each do |m_p| %>
    <%= render :partial => "item_my_music", :locals => {:music => m_p}  %>
  <% end %>
</div>

<script type="text/javascript" >
  formatLinkForPaginationURLMyPhoto();
</script>
