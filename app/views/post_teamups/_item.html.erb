<div class="assItem">
  <div class="assItemL">
    <div class="assPic"><%= show_image_user_post(post.user) %></div>
    <div class="assContT">
      <h2 class="titleH2"><%= link_to title, post_teamup_path(post.post_teamup) %></h2>
      <div class="assCate"><span>Organization:</span><%= link_to post.school.name, :controller => "post_teamups", :action => "index", :from_school => post.school, :teamup_category_id => post.post_teamup.teamup_category_id %></div>
    </div>
    <div class="assDes"><%= truncate_words(description) %></div>
  </div>
  <div class="assItemB">
    <div class="assImg"><div><%= link_to image_post_thumb(controller.controller_name, post), post.post_teamup %></div></div>
    <div class="assInfT">
      <div class="assInfT">
        <div class="assInfL">
          <div><span>Posted by:</span><%= show_user_post(post.user) %></div>
        </div>
      </div>
      <div class="assInfB">
        <div id="rate_<%= post.id %>">
          <%
            check = PostTeamup.find_rated_by(current_user).include?(post.post_teamup) if logged_in?
            check ||= false
          %>
          <div class="qashdU">
            <%= show_rating(post.id, post.post_teamup.total_good, controller.controller_name, check, 1) %>
          </div>
          <div class="qashdD">
            <%= show_rating(post.id, post.post_teamup.total_bad, controller.controller_name, check, 0) %>
          </div>
        </div>
        <div class="vie"><%= "View (#{post.count_view})" %></div>
        <div class="fav" id="item_favorite_<%= post.id %>"><%= show_favorite("Post", post) %></div>
        <div class="com"><%= link_to raw("Comments (<span id='post_#{post.id}_comments'>#{post.comments.size}</span>)"), "javascript:;", :onclick => "showListPostComment(#{post.id})" %></div>


      </div>
    </div>
  </div>
</div>
