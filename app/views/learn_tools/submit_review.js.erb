$('form#new_tool_review').before("<%= escape_javascript ( raw(render :partial => "submit_review_msg")) %>").delay(300);
setTimeout("$('a#TB_closeWindowButton').click()",1000)

/* the following statements are applied for page view Detail only*/
if ( $('form#form_tool_review').find('div.helpItem').html() != null ){
  $('form#form_tool_review').find('div.helpItem').eq(0).before("<%= escape_javascript ( raw(render :partial => "tool_review_item",:locals => {:rv => @toolreview} )) %>");
}else {
  $('form#form_tool_review').find('div.friUse').after("<%= escape_javascript ( raw(render :partial => "tool_review_item",:locals => {:rv => @toolreview} )) %>");
}

$(document).ready(function(){
  $('div#TB_ajaxContent').css('height','');
  $('div#TB_ajaxContent').css('min-height','320px');
});