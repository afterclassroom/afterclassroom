$('div#rate_id_<%= @tool.id %>').before("<%= escape_javascript ( raw(render :partial => "rating_star", :locals => {:tool => @tool, :nocheck => "no_check" })) %>");  
$('div#rate_id_<%= @tool.id %>').eq(1).remove();
vtip();