<div class="col300 Recent">
  <div class="col300in">
    <h1 class="col300Title">I need a job</h1>
    <div class="col300Cont">
      <%
      jobs_recent = PostJob.with_school(@school).recent.with_limit
    %>
      <% if jobs_recent.size > 0 %>
        <%
        jobs_recent.each do |job|
        %>
          <div class="recentItem">
            <div class="recentImg"><%= show_image_user_post(job.post.user) %></div>
            <div class="recentInf">
              <div class="recentName"><%= show_user_post(job.post.user) %></div>
              <div class="recentUni"><span>at</span><%= link_to job.post.school.name, :controller => "post_jobs", :action => "index", :from_school => job.post.school %></div>
              <div class="recentDes"><%= link_to job.post.title, post_job_path(job) %></div>
            </div>
          </div>
        <% end %>
      <% end %>
      <div class="recentBottom">&nbsp;</div>
    </div>
  </div>
</div>
