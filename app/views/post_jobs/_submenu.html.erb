<% JobType.find(:all).each do |jt| %>
<div class="menuRItem <%= "menuRselect" if @job_type_id && @job_type_id.to_i == jt.id %>"><a href="<%= "#{post_jobs_path}?job_type_id=#{jt.id}" %>"><span class="span1"><span class="span2"><%= jt.name %></span></span></a></div>
<% end %>