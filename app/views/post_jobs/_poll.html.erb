<%
post_job = PostJob.require_rating(@school).first
%>
<% if post_job %>
  <div class="col195 QAsPoll">
    <div class="col195in">
      <h1 class="col195Title">Poll this Job</h1>
      <div class="col195Cont">
        <div class="QAsPCont">
          <p><%= link_to post_job.post.title, post_job %></p>
          <p><%= truncate_words(post_job.post.description) %></p>
        </div>
        <div id="require_rating_action">
          <div class="QAsDet"><input type="radio" name="rating" value="1"/>Good <strong rel="rate" style="display:none;">(<%= post_job.total_good %>)</strong></div>
          <div class="QAsDet"><input type="radio" name="rating" value="0"/>Bad <strong rel="rate" style="display:none;">(<%= post_job.total_bad %>)</strong></div>
          <div class="submit"><%= show_submit_rating(post_job.post.id, require_rate_post_jobs_path) %></div>
          <div class="PollView" id="view_results">[<a href="javascript:;" onclick="showResult();">View results</a>]</div>
        </div>
      </div>
    </div>
  </div>
<% end %>
