<%= form_for(@help_info) do |f| %>
  <% if @help_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@help_info.errors.count, "error") %> prohibited this help_info from being saved:</h2>

      <ul>
      <% @help_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title, :size => "25" %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content, :size => "22" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
