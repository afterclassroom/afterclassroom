<div class="contMainL" style="margin-top: -12px;">
  <div class="col571 proCen" style="background: none">
    <div class="col571in" style="background: none">
      <div class="proCenCont">
        <div class="AsDcomImg">
          <%= show_image_user_post(@photo.user) %>
        </div>
        <div class="stoDetR">
          <div class="stoDetRT">
            <%= @photo.user.full_name %>: <%= @photo.photo_album.name %>
          </div>
        </div>
      </div>

      <div class="onThisBtm">
        <div class="stoPre"><%= link_to "", show_photo_detail_user_path(current_user, :photo_id => @prev), :remote => true if @prev %></div>
        <div class="stoNext"><%= link_to "", show_photo_detail_user_path(current_user, :photo_id => @next), :remote => true if @next %></div>
      </div>
      <%= render :partial => "photos/widget_photo_detail_user" %>
    </div>
  </div>
</div>

<div class="contMainR" style="margin-right: 22px;">
  <!--Most Viewed-->
  <%= render :partial => "photos/on_this_photo_user" %>
  <!--Most Viewed-->
</div>
<script>
  $(document).ready(function() {
    $('div#TB_title').filter(':gt(0)').remove();
    $('div#TB_ajaxContent').filter(':gt(0)').remove();
    $('div#TB_title').css({
      'height':'20px'
    });
    $('div#TB_window').css({
      'top':'320px'
    });
    $('#TB_ajaxContent p').css({
      'padding':'0px 0'
    });
		vtip();
  });
</script>
