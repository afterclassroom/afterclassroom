<%
  arr_rating_status = [["", "Just Posted"], ["Good", "Grade 1 Quality"], ["It's Ok", "It's Ok"], ["Bad", "Don't go There"]]
  arr_rating_status.each do |elm|
%>
<div class="menuRItem <%= "menuRselect" if @rating_status == elm.first %>"><a href="<%= "#{post_parties_path}?rating_status=#{elm.first}" %>"><span class="span1"><span class="span2"><%= elm.last %></span></span></a></div>
<% end %>