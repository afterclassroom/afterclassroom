<div class="musicL">
  <div class="photoImg"><%= link_to raw("<div>#{image_tag video.video_attach.url(:thumb)}</div>"), user_video_url(current_user, video) %></div>
  <div class="mostMSR">
    <div class="mostMSA"><%= link_to "", user_video_url(current_user, video), :class => "mostMSPlay" %></div>
  </div>
  <div class="musicB">
    <div class="musicN"><%= link_to video.title, user_video_url(current_user, video) %></div>
  </div>
</div>