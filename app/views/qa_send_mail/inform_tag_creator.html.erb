<html>
  <body>
    <div id="detail_window">
      Hello <%= @tag_creator.name %>, <br/>
      The tag of your friend <a href="<%= user_url(@user) %>"><%= @user.name %></a> has been sent to the <a href="<%= user_url(@photo.user) %>">owner</a> of this
      <%= @photo.title == "" ? raw("<a href='#{user_photo_url(@photo.user, @photo)}'>photo</a>") : "photo: "  %> 
      <a href="<%= user_video_url(@photo.user, @photo) %>"><%= @photo.title %></a>.
      
      <%= @statement %><br />
      
      Yours sincerely, <br/>
      Afterclassroom Support Team.

    </div>  
  </body>
</html>