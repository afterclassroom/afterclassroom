<html>
  <body>
    <div id="detail_window">
      You have been tagged to a <%= @photo.title == "" ? raw("<a href='#{user_photo_url(@photo.user, @photo)}'>photo</a>") : "photo: "  %> 
      <a href="<%= user_photo_url(@photo.user, @photo) %>"><%= @photo.title == "" ? "" : @photo.title  %></a> 
      
      by your friend <a href="<%= user_url(@tag_creator) %>"><%= @tag_creator.name %></a>!
      
      <%= @statement %>
    </div>  
  </body>
  
  
</html>