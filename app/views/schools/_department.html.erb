<%= select_tag("school[department_ids][]", option_groups_from_collection_for_select(@department_categories, :departments, :name, :id, :name, @school.departments.collect{|d| d.id}), { :include_blank => "Select department", :multiple => true, :id => "department_select", :style => "width:340px" }) %>