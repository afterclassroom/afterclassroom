<% content_for :menu_top_student_lounge do -%>
<%= render :partial => '/shared/submenu' %>
<% end -%>
<div class="slStoryL">
    <div class="contDetL">
        <div class="contDetLin">
            <div class="inboxLeft">
                <div class="slsShare">
                    <div class="slsShareL">
                        Update Photo Albums
                    </div>
                    <div class="slsShareR">
                        <%= render :partial => "photos/menu" %>
                    </div>
                </div>
                <%= display_flash_messages_with_raw %>
                <%= render :partial => "photos/expand_create_photo_album" %>
                <div id="my_photos_list">
                    <div class="slsTop">
                        <div class="photoRow" id="mytable">
                            <% @my_photo_albums.each do |m_p| %>
                            	<%= render :partial => "item_my_album", :locals => {:album => m_p} %>
                            <% end %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="contMainR">
    <!--Most Viewed-->
    <%= render :partial => "photos/most_viewed" %>
    <!--Most Viewed-->
    <!--Ads-->
    <%= render :partial => "ads/show_vertical_ads" %>
    <!--Ads-->
</div>

<%= render :partial => "action_box_menu" %>
