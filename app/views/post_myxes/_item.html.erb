<div class="assItem">
    <div class="assItemL">
        <div class="assPic">
            <%= show_image_user_post(post.user) %>
        </div>
        <div class="assContT">
            <h2 class="titleH2"><%= link_to title, post_myx_path(post.post_myx) %></h2>
            <div class="assCate">
                <span>From:</span>
                <%= link_to post.school.name, :controller => "post_myxes", :action => "index", :from_school => post.school %>
            </div>
            <div class="assDes">
                <div class="assImg">
                    <div>
                        <%= link_to image_post_thumb(controller_name, post), post.post_myx %>
                    </div>
                </div>
                <%= truncate_html(description, :length => 250, :omission => '...') %>
            </div>
        </div>
    </div>
    <div class="assItemB">
        <div class="assInfT">
            <div class="assInfT">
                <div class="assInfL">
                    <div>
                        <span>Posted by:</span>
                        <%= show_user_post(post.user) %>
                    </div>
                </div>
            </div>
            <div class="assInfB">
                <div id="rate_<%= post.id %>">
                    <%
                    check = PostMyx.find_rated_by(current_user).include?(post.post_myx) if logged_in?
                    check ||= false %>
                    <div class="qashdU">
                        <%= show_rating(post.id, post.post_myx.total_good, get_controler_name_from_class_name(post.post_myx.class.name), check, 2) %>
                    </div>
                    <div class="cheap">
                        <%= show_rating(post.id, "Bored(#{post.post_myx.total_bored})", get_controler_name_from_class_name(post.post_myx.class.name), check, 1) %>
                    </div>
                    <div class="qashdD">
                        <%= show_rating(post.id, post.post_myx.total_bad, get_controler_name_from_class_name(post.post_myx.class.name), check, 0) %>
                    </div>
                </div>
                <div class="vie">
                    <%= "View (#{post.count_view})" %>
                </div>
                <div class="fav" id="item_favorite_<%= post.id %>">
                    <%= show_favorite("Post", post) %>
                </div>
                <div class="com">
                    <%= show_comment_button(post.id, post.comments.size) %>
                </div>
            </div>
        </div>
    </div>
    <!--Comments-->
    <%= render :partial => "shared/post_comments/show_list_comments", :locals => {:post => post} %>
    <!--Comments-->
</div>
